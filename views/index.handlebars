<h1>Burger Destroy</h1>
<h2>Add a burger, destroy a burger.</h2>
<ul>
    {{#each burgers}} {{#unless devoured}} {{> cats/cat-block sleep=true}} {{/unless}} {{/each}}
</ul>
<h2></h2>
<ul>
    {{#each burgers}} {{#if devoured}} {{> cats/cat-block sleep=false}} {{/if}} {{/each}}
</ul>
<h2>Add a burger</h2>
<form class="create-form">
    <div class="form-group">
        <label for="ca">Cat Name:</label>
        <input type="text" id="ca" name="name">
    </div>
    <div class="form-group">
        <label for="devoured">devoured?</label>
        <br>
        <input type="radio" name="devoured" value="1" checked> Devoured!
        <br>
        <input type="radio" name="devoured" value="0"> Awake
    </div>
    <button type="submit">Add Cat</button>
</form>